# 강사님 피드백 ↔ 현재 작업 상황 갭 분석 및 파트별 강화 계획 v1.0

| 항목 | 값 |
| ----- | ----- |
| 작성자 | 이동원 (Competitor Agent 담당 / PM) |
| 기준일 | 2026-06-13 (KST) |
| 피드백 원문 | [`2026-05-31_강사검토리포트_d24b475.md`](2026-05-31_강사검토리포트_d24b475.md) — 20/70 (F), RAGAS 게이트 발동 |
| As-Is 실측 기준 | `main` 최신 커밋 `212dd35` (2026-06-13 코드 grep·파일 실측) |
| 진행도 대시보드 | `docs/roadmap/2026-06-12/progress_dashboard.html` (현재 자체 추정 23/70 → 목표 46/70) |
| 방법론 | Gap Analysis (As-Is → To-Be → Gap → Action) + Evidence-grounding(file:line) + Impact×Effort 우선순위 |

> **이 문서의 모든 "현재 상태"는 2026-06-13 기준 코드 실측 결과입니다.**
> 강사님 리포트와 같은 방식(증거 강제, 선언≠실동작)으로 자체 채점했으며, 추정·희망 표현은 배제했습니다.

---

## 1. 방법론

1. **As-Is 실측**: 루브릭 10항목 각각에 대해 리포트 시점(d24b475) 지적사항이 현재 main(212dd35)에서 해소됐는지 grep·파일 존재·실행 결과로 검증.
2. **To-Be 정의**: 강사님 리포트의 "보완 필요(우선순위 순)" 7개 액션을 항목별 목표 점수로 환산.
3. **Gap 도출**: 항목별 잔여 격차를 담당 파트에 매핑 (README §5 에이전트 작업 분리 기준 준수).
4. **Action 설계**: 파트별로 ①중점 강화(이미 있는 것을 올리는 작업) ②신규 구현(없는 것을 만드는 작업)을 분리하고, 완료 기준(DoD)과 예상 점수 효과를 명시.
5. **우선순위**: 가중치(×2 항목 우선) × 구현 난이도로 P0~P3 부여.

---

## 2. 루브릭 10항목 As-Is 실측 (2026-06-13)

| # | 항목 | 가중 | 리포트(05-31) | 현재 실측 | 실측 증거 (2026-06-13) | 잔여 갭 |
|---|------|:---:|:---:|:---:|------|------|
| 1 | Multi-Agent 구조 | ×2 | 2 | **2 (정체)** | `graph/pipeline.py:73-84` 여전히 8개 순차 동기 호출. langgraph는 주석 1건뿐(`pipeline.py:77` "future LangGraph"), 의존성 미설치 | LangGraph 전환 미착수. **신규 발견: Macro Agent(PR #43)가 `pipeline.py`에 미연결** — `run_macro` 호출 0건 |
| 2 | 에러핸들링 & 폴백 | ×2 | 3 | **3 (진행중, 회귀 1건)** | OpenRouter 재시도 머지(PR #40). 그러나 `agents/qual.py:1`이 `rag.retriever`를 fallback 없이 직접 import → CI에서 테스트 3건 제외 중(`.github/workflows/ci.yml:39-41`) | qual fallback 회귀 해소 + CI 3건 재활성화가 4점 확정의 선행 조건 |
| 3 | sLLM + 검증 + langfuse | ×2 | 2 | **2 (정체)** | langfuse import 0건. `config.py:21-24` LangSmith placeholder만 존재. `agents/guardrail.py:27` `passed=True` 고정 그대로 | 검증 게이팅·트레이싱 둘 다 미착수 |
| 4 | 5대 설계문서 | ×1 | 3 | **3 (유지·개선)** | 5종 유지 + 진행도 대시보드(PR #41) + UI 가이드(PR #45) 추가. 리포트가 지적한 "eval/ 허위 파일트리"는 PR #42로 실파일 존재하게 되어 해소 | 문서-코드 정합 유지 (graph/README의 StateGraph 주장은 전환 전까지 "목표" 표기 필요) |
| 5 | Dockerise | ×1 | 2 | **3 (개선)** | peer_tool 머지충돌 해소 + CI 신설(PR #39, compileall+pytest 게이트) + psycopg2 의존성 드리프트 해소 | Dockerfile 멀티스테이지화 + HEALTHCHECK 미적용 (→4점 조건) |
| 6 | MCP / A2A | ×1 | 0 | **0 (미착수)** | src 전체 grep 0건 | 담당 미배정 |
| 7 | vLLM | ×1 | 0 | **0 (미착수)** | src·pyproject grep 0건 | 담당 미배정 |
| 8 | RAGAS 정량평가 | ×2 | 0 | **3 (개선, 게이트 해소)** | `eval/golden_set/personas.json`·`expected_outputs.json`·`run_benchmark.py` 실존재. 실데이터 DB 실측 리포트 커밋(`eval/reports/2026-06-12_benchmark.md`): rule-based 40/41, **faithfulness 0.4096** | 목표 0.80 대비 절반 수준. Qual RAG 실연결이 선행 조건 |
| 9 | RAG 고도화 | ×1 | 2 | **2 (정체)** | rerank/bm25/ts_rank/rrf grep 0건. DB 인덱스(`search_tsv`, gin_trgm)는 깔려 있으나 코드 미사용 그대로 | hybrid + reranker 연결 미착수 |
| 10 | 스트리밍 & 비동기 | ×1 | 1 | **1 (미착수)** | `async def`·`asyncio` grep 0건 | 미착수, 담당 미배정 |

**종합**: 리포트 20/70(F) → 현재 자체 추정 **23/70** (대시보드 기준). 가장 큰 성과는 **항목 8 게이트 해소(0→3)**. 가장 큰 잔여 리스크는 **가중 ×2 항목 3개(#1, #2, #3)가 전부 정체 또는 회귀 상태**라는 점.

### 이번 실측에서 새로 발견된 사항 (대시보드 미반영)

1. **Macro Agent 파이프라인 미연결** — `agents/macro.py`는 머지됐지만(PR #43) `graph/pipeline.py:73-84`의 실행 순서에 `run_macro`가 없음. 구현은 됐는데 실행 경로에 없으면 강사님 채점 기준으로는 "선언 수준"으로 강등될 수 있음.
2. **전 골든셋 케이스 signal=HOLD** — `eval/reports/2026-06-12_benchmark.md`의 5케이스 모두 HOLD. 매수/매도 분별력이 없다는 신호로, Strategist 종합 로직의 변별력 점검 필요 (RAGAS와 별개의 품질 이슈).
3. **faithfulness 0.4096의 원인 가설** — Qual이 RAG 미연결 상태라 답변의 근거(contexts)가 빈약함. Qual RAG 연결(P0) 없이는 0.80 도달이 구조적으로 어려움.

---

## 3. 파트별 강화·구현 계획

> 표기: 🔴 **P0** 즉시(다른 작업의 선행 조건) · 🟠 **P1** 점수 레버리지 최대(×2 항목) · 🟡 **P2** 점수 효율 양호 · ⚪ **P3** 최소구현 전략

### 3.1 Qual 담당 — 🔴 P0 (팀 전체의 병목)

| 구분 | 내용 |
|------|------|
| **중점 강화** | `qual.py:1`의 retriever 직접 import에 try/except + mock fallback 복원. 리포트가 "이 프로젝트 최고 강점"(항목 2)으로 꼽은 폴백 일관성이 깨진 유일한 회귀 지점 |
| **신규 구현** | Qual이 `rag_documents`/`rag_chunks`를 실제 검색해 답변 근거(contexts)로 사용 — RAGAS faithfulness 0.41→0.80의 구조적 선행 조건 |
| **DoD** | ① CI 제외 테스트 3건(`ci.yml:39-41`) 재활성화 후 통과 ② DB 없는 환경에서 파이프라인이 크래시 없이 fallback 동작 ③ `run_benchmark.py` 재실행 시 contexts에 실제 검색 결과 포함 |
| **점수 효과** | #2: 3→4 확정 (×2 = +2), #8 개선의 선행 조건 |

### 3.2 Graph 담당 — 🟠 P1 (단일 작업으로 최대 레버리지)

| 구분 | 내용 |
|------|------|
| **중점 강화** | (즉시·소규모) **Macro Agent를 `pipeline.py` 실행 순서에 연결** — 이미 구현된 자산이 채점에 잡히도록. Curator 이후, Strategist 이전에 배치 |
| **신규 구현** | LangGraph `StateGraph` 전환: `add_node` 9개(Macro 포함) + Curator 후 Quant/Qual/Competitor/Macro `Send` fan-out + Strategist merge + Guardrail conditional edge(needs_revision→재합성). 리포트 보완 4번 항목 그대로 |
| **DoD** | ① `pyproject.toml`에 langgraph 추가 ② `graph/README.md`의 "StateGraph" 주장이 실코드와 일치 ③ fan-out 병렬 실행 확인 ④ 기존 테스트 + CI 통과 |
| **점수 효과** | #1: 2→4 (×2 = +4), #10 비동기 가점 동반 (+1) — **단일 작업 최대 +5** |

### 3.3 Guardrail 담당 — 🟠 P1

| 구분 | 내용 |
|------|------|
| **중점 강화** | `guardrail.py:27`의 `passed=True` 고정 제거 → 근거 부족·스키마 위반 시 `passed=False`로 출력 게이팅 + Strategist 재시도 트리거 (리포트 보완 5번) |
| **신규 구현** | langfuse 실연결 — 요청 1건당 trace/span 적재. `config.py:21-24`의 LangSmith placeholder를 실동작 트레이싱으로 교체 (langfuse·LangSmith 중 택1, 루브릭은 langfuse 명시) |
| **DoD** | ① guardrail 실패 케이스 테스트 추가(passed=False 경로) ② langfuse 대시보드에서 trace 확인 스크린샷 ③ sLLM 호출 4경로(Curator·Classifier·Qual·Analyst) span 포함 |
| **점수 효과** | #3: 2→4 (×2 = +4) |

### 3.4 이동원 본인 (Competitor / PM / 평가·CI) — 🟠 P1 + PM 조율

**직접 작업:**

| 구분 | 내용 |
|------|------|
| **중점 강화 (#8 RAGAS)** | ① 골든셋 5→10케이스 확대(매수/매도/위험 시나리오 추가 — 전 케이스 HOLD 문제 검증용) ② answer_relevancy 지표 추가 ③ Qual RAG 연결 후 faithfulness 재측정·리포트 누적(`eval/reports/`) — 0.80 목표 추적 그래프 |
| **중점 강화 (#5 CI/Docker)** | Dockerfile 멀티스테이지화 + `HEALTHCHECK` + compose app healthcheck 추가 (리포트 보완 2번 잔여분) — #5: 3→4 |
| **중점 강화 (Competitor)** | OpenRouter 모델 퇴역(404) 재발 방지 — 모델명 설정 외부화 + 호출 실패 시 모델 폴백 체인. peer 비교 narrative 품질 유지 |
| **신규 구현 (PM)** | 회귀 추적 자동화: `run_benchmark.py`를 CI에서 주기 실행(또는 PR 라벨 트리거)해 faithfulness 하락 감지 |
| **DoD** | ① 골든셋 10케이스 + BUY/SELL 케이스에서 HOLD 아닌 신호 검증 ② faithfulness 추이가 리포트로 누적 ③ Docker 이미지 내 `import stock_agent.graph.pipeline` 성공(강사님 import smoke 기준) |
| **점수 효과** | #8: 3→4 (×2 = +2), #5: 3→4 (+1) |

**PM 조율 (코드 외 책임):**

1. **미배정 항목 배정** — #6 MCP/A2A, #7 vLLM, #10 스트리밍이 미배정 상태. 다음 주간 회의에서 담당 확정 (3.8 최소구현 전략 참조).
2. **문서-코드 정합 게이트** — 리포트가 "허위 파일 트리"를 명시 감점한 만큼, PR 리뷰 시 README 주장 ↔ 실코드 일치 여부를 체크리스트화. `graph/README.md:3`의 StateGraph 주장은 전환 전까지 "목표" 표기로 수정 지시.
3. **Curator corp_code 보정(PR #44) 사후 리뷰** — 영역 외 수정 공지대로 Curator 담당자의 승인/대안(DB lookup) 결정 회수.
4. **강사님 재검토 요청 시점 판단** — P0+P1 완료 시 추정 38~40/70(D~C권). 재검토는 P1까지 머지된 뒤가 효율적.

### 3.5 RAG 담당 — 🟡 P2

| 구분 | 내용 |
|------|------|
| **중점 강화** | 이미 깔린 DB 인프라(`db/init/002...sql`의 `search_tsv`·gin_trgm) 활용 — 리포트 표현 그대로 "코드만 잇는 작업" |
| **신규 구현** | ① `ts_rank` 키워드 점수 + pgvector cosine 점수의 RRF 융합 ② `bge-reranker`(CrossEncoder)를 `retriever.py` 말미에 연결 |
| **DoD** | ① hybrid on/off 비교 검색 품질 테스트 ② reranker 적용 전후 RAGAS context_precision 비교 수치 |
| **점수 효과** | #9: 2→4 (+2), #8 faithfulness 개선 간접 기여 |

### 3.6 Macro 담당 (melinamuun) — 🟡 P2

| 구분 | 내용 |
|------|------|
| **중점 강화** | Graph 담당과 협업해 `pipeline.py` 연결 (3.2의 즉시 작업) — 연결 자체는 Graph 영역이므로 **Macro 결과를 Strategist가 실제 소비하도록** `strategist.py` 입력에 `state.macro` 반영 |
| **신규 구현** | Macro 평가가 signal/confidence에 영향을 주는 가중 로직 (전 케이스 HOLD 문제 해소에도 기여) |
| **DoD** | ① E2E 실행 시 결과에 macro 평가 포함 ② macro 유무에 따라 confidence가 달라지는 테스트 |
| **점수 효과** | #1 Multi-Agent 실체성 보강 (선언→실동작 근거) |

### 3.7 Curator / Quant / Strategist 담당 — 🟡 P2

| 담당 | 작업 |
|------|------|
| **Curator** | PR #44(corp_code 보정) 리뷰·승인 또는 DB lookup 방식으로 대체 결정. 카탈로그 외 종목 입력 시 corp_code 해소 경로 정의 |
| **Quant** | DART 재무 조회 실패 시 재시도(retry) 루프 추가 — 리포트가 #2를 3점에 묶은 사유("재시도 루프 없음") 해소. corp_code 정상화로 실데이터 경로가 열린 만큼 mock 의존 축소 |
| **Strategist** | 전 케이스 HOLD 문제 분석 — 신호 산출 로직의 변별력 점검. Quant/Qual/Competitor/Macro 결과 가중 종합 규칙을 명시적 코드로. Guardrail `needs_revision` 시 재합성 경로 구현(3.3과 계약 맞춤) |

### 3.8 미배정 항목 — ⚪ P3 (최소구현 전략, PM 배정 필요)

세 항목 모두 0~1점이라 **소규모 실동작 1개**만으로도 점수 효율이 높음:

| 항목 | 최소구현 제안 | 예상 효과 |
|------|------|------|
| #10 스트리밍·비동기 | Streamlit `st.write_stream` + LLM `stream=True` 토큰 점진 출력. LangGraph 전환(3.2) 시 fan-out을 `asyncio.gather`로 — Graph 담당과 묶으면 비용 최소 | 1→3 (+2) |
| #6 MCP/A2A | 외부 데이터 Tool 1개(예: DART/주가 조회)를 MCP 서버로 노출하거나 기존 공개 MCP 클라이언트 연결 — "실동작 1경로" 확보 | 0→2 (+2) |
| #7 vLLM | 로컬 소형 모델 1개를 vLLM으로 서빙해 Guardrail 검증 등 1개 경로에 연결하는 데모. 불가 시 ADR로 "외부 API 선택 사유" 명문화(0점이어도 감점 방어) | 0→2 (+2) |

---

## 4. 우선순위 로드맵 요약

| 순위 | 작업 | 담당 | 점수 효과(가중) | 비고 |
|:---:|------|------|:---:|------|
| 🔴 P0-1 | qual fallback 복원 + CI 3건 재활성화 | Qual | +2 | #8 개선의 선행 조건 |
| 🔴 P0-2 | Macro `pipeline.py` 연결 | Graph(+Macro) | (실체성 방어) | 소규모·즉시 가능 |
| 🟠 P1-1 | LangGraph StateGraph 전환 + fan-out | Graph | +4~5 | 최대 레버리지 |
| 🟠 P1-2 | Guardrail 게이팅 + langfuse | Guardrail | +4 | |
| 🟠 P1-3 | RAGAS 0.80 추진(골든셋 확대·재측정) + Docker 마감 | 이동원 | +3 | Qual RAG 연결 후 재측정 |
| 🟡 P2 | RAG hybrid/reranker, Macro→Strategist 반영, retry, HOLD 변별력 | RAG·Macro·Quant·Strategist | +2~3 | |
| ⚪ P3 | 스트리밍, MCP, vLLM 최소구현 | 미배정(PM 배정) | +4~6 | 배정이 선행 |

**예상 궤적**: 현재 23/70 → P0+P1 완료 시 약 36~38/70 → P2~P3 완료 시 **목표 46/70 (65.7%)** 도달.

---

## 5. 변경 이력

| 날짜 | 버전 | 변경 요약 |
|------|------|-----------|
| 2026-06-13 | v1.0 | 최초 작성 — 2026-05-31 강사 리포트 대비 As-Is 실측(212dd35) + 파트별 강화 계획 |
