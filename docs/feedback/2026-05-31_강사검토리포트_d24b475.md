# 검토 리포트 — BDAI_Pocat_Team1 (stock-agent) @ d24b475

- 모드: **Full (정적 + 동적)** — Docker 빌드/기동 동적 검증 수행
- 검토 일시: 2026-05-31
- 대상 경로: `D:/git/lecture_proj_review/BDAI_Pocat_Team1/stock-agent` (프로젝트 루트는 `stock-agent/`)
- 커밋 sha: `d24b475` (git 저장소)
- 동적 로그: `/tmp/revlogs/build.log`, `/tmp/revlogs/import_smoke.log` (샌드박스 격리 실행, 비밀키 미주입, `--network none`, 리소스 제한 후 teardown 완료)
- 종합: **20 / 70 (28.6%) → 등급 F**
- ⚠ **GATE: 항목8 RAGAS = 0 (정량 평가 전무) → 등급 상한 B** (적용되나 percent 자체가 F 구간이라 상한이 결정적이지 않음)

---

## 종합 요약

문서는 **부트캠프 상위권 수준으로 방대하고 정직**(현재 vs 목표를 일관되게 구분)하지만, **코드는 "Phase 1 mock skeleton" 자기 선언 그대로** 멈춰 있다. 루브릭 핵심 4항목 중 항목8(RAGAS)은 코드가 전무하고, 항목3의 langfuse, 항목6 MCP/A2A, 항목7 vLLM, 항목10 스트리밍·비동기는 **선언만 있거나 부재**다. 결정적으로 **`peer_tool.py`에 미해결 Git 머지 충돌 마커(`<<<<<<< HEAD`)가 커밋되어 파일이 파싱조차 되지 않으며**, Docker 이미지에 런타임 의존성(`pgvector`/`sentence_transformers`/`openai`/`psycopg2`)이 빠져 있어 실제 분석 경로는 컨테이너에서 동작하지 않는다.

빛나는 부분은 **항목2 에러핸들링/폴백**(각 워커의 DB 실패 시 mock fallback이 실제로 구현·동작)과 **설계 문서 5종의 완비성**이다. 취업 관점에서 이 팀의 강점은 "기획·설계 역량"이고, 약점은 "선언을 실동작으로 옮기는 구현 마감"이다.

---

## 항목별 스코어카드

| # | 항목 | 점수 | 가중 | 가중점 | 신뢰 | 증거 (file:line / cmd) | 코멘트 |
|---|------|:---:|:---:|:---:|:---:|------|------|
| 1 | Multi-Agent 구조 | 2 | ×2 | 4 | [S] | `graph/pipeline.py:73-84` 8개 에이전트를 **순차 함수 호출**로 실행, supervisor 라우팅·fan-out 없음. `agents/strategist.py:58` Strategist가 종합(수렴점)은 함. pyproject에 langgraph 부재(grep), `graph/README.md:3`은 StateGraph 주장 | 패턴의 "형태"는 있으나(공유 `AgentState` + Strategist 수렴) 실체는 하드코딩 순서. 워커는 mock. 선언적(2). |
| 2 | 에러핸들링 & 폴백 | 3 | ×2 | 6 | [S] | `agents/quant.py:30-44` DB 실패 시 mock metrics fallback, `agents/qual.py:113-135,166-181` DB·LLM 실패 fallback, `agents/competitor.py:40-44,277-280` 예상 DB 오류만 fallback·그 외 re-raise, `agents/investment_analyst.py:67-75` GLM 실패 시 mock Strategist 유지 | **이 프로젝트 최고 강점.** 실패 사유를 결과에 기록하는 설계가 일관됨. 견고(3). 재시도(retry) 루프는 없어 4 미달. |
| 3 | sLLM + 검증 Agent + langfuse | 2 | ×2 | 4 | [S] | (a) sLLM: `config.py:27` `glm-4.5-flash`(z.ai, 300B↓ 게이트웨이 → sLLM 인정), `agents/curator.py:83`·`request_classifier.py:86`·`qual.py:152`·`investment_analyst.py:60` 실제 호출 경로 연결 [S]. (b) 검증: `agents/guardrail.py:18-24` 금지어 치환·warning은 rule-based로 **실제 동작**하나 출력 재시도/게이팅(passed=False)은 없음(`:26` 항상 passed=True). (c) langfuse: **부재** — `config.py:21-24` LangSmith placeholder만, src 어디에도 langfuse/langsmith import 없음(grep), `README.md:48` "준비 중" 자인 | 세 요소 중 sLLM만 실동작. 트레이싱 0, 검증 게이팅 미흡 → 핵심 항목인데 선언 수준(2). |
| 4 | 5대 설계문서 | 3 | ×1 | 3 | [S] | (1)요구사항 `docs/prd/PRD_v0.6.md` ✅ (2)기능명세 `docs/functional-spec/overview/functional_spec_all_features_v0.1.md` 11기능 7항목 양식 ✅ (3)아키텍처 `docs/architecture/multi_agent_architecture.md`·`system_flow.md`(Mermaid 4종) ✅ (4)ERD `docs/architecture/erd.md` ✅ (5)시퀀스/인터페이스 `system_flow.md:56-94` Sequence ✅ — **5대 모두 존재(누락 0, 캡 미적용)** | 존재성·가독성은 A급. 단 **코드-문서 불일치로 감점**(아래 교차검증). 정직한 "현재 vs 목표" 표기는 가점 요소. |
| 5 | Dockerise | 2 | ×1 | 2 | [D] | `docker compose build app` **성공**(~30s, build.log), streamlit 컨테이너 기동 확인(`Uvicorn server started on 0.0.0.0:8501`). 그러나 `Dockerfile:1-17` 단일스테이지·HEALTHCHECK 없음, app 서비스 compose healthcheck 없음. **치명**: import_smoke.log — `pgvector`/`sentence_transformers` 미설치로 RAG 경로 ModuleNotFoundError, `peer_tool.py:495` SyntaxError로 파이프라인 import 실패 | 빌드·서버 기동은 [D] 확인되나 실제 앱 로직이 컨테이너에서 깨짐. pyproject deps ≠ 코드 실제 import. 견고성 결여(2). |
| 6 | MCP / A2A | 0 | ×1 | 0 | [S] | src·streamlit_app 전체 grep: MCP/A2A 구현 없음. `graph/README.md:31` "MVP에서 A2A 미도입", `streamlit_app.py:19` "10주차 A2A 패턴"(미래 계획) | 미래 계획 언급만. 코드 부재(0). |
| 7 | vLLM | 0 | ×1 | 0 | [S] | pyproject·src 전체 grep: vllm import·설정 부재. LLM 서빙은 외부 API(z.ai GLM, OpenRouter)만 사용 | vLLM 흔적 자체가 없음(환경 한계 아님). 0. |
| 8 | RAGAS 정량 평가 | 0 | ×2 | 0 | [S] | **전무.** ragas import 0(repo 전체 grep), `eval/run_benchmark.py`·`personas.json`·`expected_outputs.json` **부재**(find 확인, 실제 eval/는 README+`.gitkeep` 2개뿐). `eval/README.md:18-27`·`multi_agent_architecture.md:526`은 RAGAS Faithfulness 0.80 목표를 주장하나 데이터셋·스크립트·점수 산출 코드 모두 없음 | 핵심 항목인데 0. **소프트게이트 발동(등급 상한 B).** |
| 9 | RAG 고도화 (Hybrid·Reranker) | 2 | ×1 | 2 | [S] | 벡터검색 `rag/pgvector_store.py:18-53`·`rag/retriever.py:16-80` pgvector cosine 단일 경로 존재[S]. Hybrid/BM25/RRF/Reranker **부재**(src 전체 grep 0건). DB엔 `db/init/002...sql:45,55-58` `search_tsv`·`gin_trgm` 인덱스가 있으나 **코드가 사용 안 함**. `rag/README.md:9,20` reranker "후속 예정" 자인 | 단순 벡터검색만(선언+기본). 인프라(tsv/trgm)는 깔렸으나 미연결. 2. |
| 10 | 스트리밍 & 비동기 | 1 | ×1 | 1 | [S] | `async def`·`asyncio`·`await`·`gather` src 전체 0건. `write_stream`/SSE 0건. `pipeline.py:73-84` 완전 동기 순차. `system_flow.md:83` "st.spinner 실시간 표시" 주장하나 진행 표시 UI 수준(토큰 스트리밍 아님) | 스트리밍·비동기 구현 없음. 계획만(1). |

**가중 합계 = 4+6+4+3+2+0+0+0+2+1 = 20 / 70 → 28.6% → 등급 F**

---

## 교차검증 (문서 주장 ↔ 코드 실태) — 코드 우선, 항목4 감점 근거

| 문서 주장 | 코드 실태 | 판정 |
|------|------|------|
| `graph/README.md:3` "6개 에이전트를 **LangGraph StateGraph**로 연결", `system_flow.md:98-138` "Send API 병렬 fan-out" | `pipeline.py:73-84` 순차 동기 함수 호출, langgraph 미설치 | **불일치** — 단, `multi_agent_architecture.md:89-100`·`system_flow.md:34-49`에서 "현재 mock / 목표 LangGraph"로 **정직히 구분**해 감점 폭 완화 |
| `eval/README.md:7-14` `run_benchmark.py`·`personas.json`·`expected_outputs.json` 존재 | 실제 `eval/`는 `.gitkeep`만, 파일 부재 | **불일치(허위 파일 트리)** — 항목4·8 감점 |
| `functional_spec...v0.1.md:312-316`·`system_flow.md:140` "Hybrid Search + Reranker + RAGAS 백그라운드 채점" | 코드 부재 | **불일치** — 항목8·9 감점 |
| `erd.md:179-281` `company`/`financial_data`/`disclosure`/`stock_price` 스키마 | `db/init/001`·`002` 실제 SQL과 대체로 정합(`rag_documents`/`rag_chunks` vector(1024) 일치) | **대체로 일치** — ERD는 신뢰 가능 (가점) |
| README/구조도가 묘사하는 안정 동작 | `peer_tool.py:495-527,533-570,689-693` **미해결 머지 충돌 마커 커밋** → 파일 SyntaxError | **치명적 코드 결함** — 문서가 가린 미완성. 항목1·5 감점 |

> 문서 품질 자체는 높으나, "그럴듯한 문서가 미구현·깨진 코드를 가리는" 구조가 일부 확인되어 항목4를 5가 아닌 3으로 한정.

---

## 강점 (취업 관점)

1. **에러핸들링·폴백 설계의 일관성** (항목2) — 모든 워커가 DB/LLM 실패를 잡아 mock fallback으로 흐름을 유지하고 **사유를 결과에 기록**한다(`quant.py`, `qual.py`, `competitor.py`, `investment_analyst.py`). 프로덕션 사고방식의 흔적. 면접에서 강조할 만함.
2. **설계 문서 완비성·정직성** (항목4) — PRD, 기능명세 11종, 아키텍처(Mermaid 4종), ERD, ADR 4건까지 구비. 특히 "현재 구현 vs 목표 구현"을 모든 문서에서 일관되게 분리한 정직함은 평가자 신뢰를 얻는다.
3. **에이전트 책임 분리·계약 설계** — 공유 `AgentState`(Pydantic) + `agents/`(판단)·`tools/`(I·O 계산) 분리 + 프롬프트 `.md` 외부화(`prompts/*/system.md`)는 협업 구조로서 합리적.
4. **sLLM 실연결** (항목3 부분) — GLM(glm-4.5-flash) 호출이 Curator·Classifier·Qual·Analyst 실행 경로에 실제로 연결되고, 키 부재 시 graceful fallback까지 갖춤.

---

## 보완 필요 (우선순위 순 — 무엇을 고치면 어느 항목이 오르는가)

1. **[즉시·항목5·1] `peer_tool.py` 머지 충돌 3곳 해소**(`:495-527`, `:533-570`, `:689-693`). 현재 파일이 파싱 불가라 competitor/peer 전 경로가 죽는다. `python -m py_compile`을 CI에 추가하면 재발 방지. → 해소 시 항목5 2→3, 항목1 신뢰도 회복.
2. **[항목5] Dockerfile 런타임 의존성 일치** — `qual.py`/`retriever.py`가 쓰는 `pgvector`, `sentence-transformers`, `openai`, `psycopg2-binary`를 `pyproject.toml [dependencies]`에 추가. 현재 이미지는 이들이 없어 RAG 경로가 컨테이너에서 ImportError. + Dockerfile 멀티스테이지화 + app HEALTHCHECK 추가. → 항목5 2→4.
3. **[핵심·항목8] RAGAS 파이프라인 실구현** — `eval/golden_set/personas.json`(현재 부재) 실제 작성 + `eval/run_benchmark.py`에서 `ragas.evaluate`(faithfulness·answer_relevancy)를 **재실행 가능**하게 + 산출 리포트를 `eval/reports/`에 커밋. 데이터셋·스크립트·수치 재현이 핵심. → 항목8 0→3, 소프트게이트 해제로 등급 상한 상승.
4. **[핵심·항목1] LangGraph 실전환** — 선언만 된 StateGraph를 실제로 구성: `add_node`(8 에이전트) + Curator 후 Quant/Qual/Competitor `Send` fan-out + Strategist merge + Guardrail conditional edge(needs_revision→재합성). mock 워커를 실제 DB/Tool 연결로 교체. → 항목1 2→4, 항목10 비동기 가점 동반.
5. **[핵심·항목3] langfuse(또는 LangSmith) 실연결 + 검증 게이팅** — placeholder를 넘어 실제 trace 적재(요청 1건당 span 생성). Guardrail이 근거 부족·스키마 위반 시 `passed=False`로 **출력을 게이팅하고 Strategist 재시도**를 트리거하도록(`guardrail.py:26` 항상 True 제거). → 항목3 2→4.
6. **[항목9] RAG 하이브리드·리랭커** — 이미 깔린 `search_tsv`(`ts_rank`)와 벡터 점수를 RRF로 융합하고, `bge-reranker`(CrossEncoder)를 retriever 말미에 연결. DB 인프라는 준비됐으니 코드만 잇는 작업. → 항목9 2→4.
7. **[항목10] 스트리밍·비동기** — Streamlit `st.write_stream` + LLM `stream=True` 제너레이터로 토큰 점진 출력, fan-out 워커는 `asyncio.gather`로 동시 실행. → 항목10 1→3.

---

## 재현 정보

- 커밋: `d24b475` (git `rev-parse --short HEAD`)
- 모드: Full — 동적 검증은 격리 컨테이너(`--network none --memory 2g --cpus 2 --pids-limit 256 --cap-drop ALL --security-opt no-new-privileges`)에서 수행, 평가자 비밀키 미주입, 검토 후 `docker compose down -v` + 이미지 삭제 완료.
- 핵심 동적 결과: `docker compose build app` 성공(~30s) / streamlit Uvicorn 기동 확인 / `peer_tool.py:495` SyntaxError(머지 충돌) / RAG 모듈 ImportError(deps 누락) — 로그 `/tmp/revlogs/build.log`, `/tmp/revlogs/import_smoke.log`.
- 채점 기준: `.claude/skills/rubric-scoring/SKILL.md`(가중·등급·게이트), `evidence-grounding`(선언2 vs 실동작3, 증거 강제). temperature 0 결정론 채점.
